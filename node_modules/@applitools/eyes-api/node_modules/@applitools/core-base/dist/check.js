"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeCheck = void 0;
const transform_image_1 = require("./utils/transform-image");
function makeCheck({ requests, logger: defaultLogger }) {
    return async function check({ target, settings = {}, logger = defaultLogger, }) {
        logger.log('Command "check" is called with settings', settings);
        if (!target.isTransformed) {
            target.image = await (0, transform_image_1.transformImage)({ image: target.image, settings });
        }
        return requests.check({ target, settings, logger });
    };
}
exports.makeCheck = makeCheck;
